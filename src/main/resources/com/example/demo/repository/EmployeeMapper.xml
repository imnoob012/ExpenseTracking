<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.EmployeeMapper">
	
	<insert id="employeeRegister">
		INSERT INTO USERS(NAME, MAIL, PASSWORD, CREATEDATE, ROLE) VALUES (#{name}, #{mail}, #{password}, CURRENT_TIMESTAMP, #{role});
	</insert>
	
	<select id="findAllEmployees">
		SELECT * FROM USERS;
	</select>
	
	<select id="getEmployeeById">
		SELECT * FROM USERS WHERE ID = #{id};
	</select>
	
	<update id="updateEmployee">
		UPDATE USERS
		<set>
			NAME = #{name},
			MAIL = #{mail},
			ROLE = #{role},
			UPDATEDATE = CURRENT_TIMESTAMP
		</set>
		WHERE ID = #{id};
	</update>
	
	<update id="deleteEmployee">
		UPDATE USERS
		SET DELETEDATE = CURRENT_TIMESTAMP
		WHERE id = #{id}
		AND DELETEDATE IS NULL;
	</update>
	
	<select id="findEmployeeByMail">
		SELECT MAIL, PASSWORD, ROLE FROM USERS WHERE MAIL = #{MAIL};
	</select>
	
</mapper>


